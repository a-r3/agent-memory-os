# Codex Runtime Contract Template

Use the canonical MCP flow:

1. compile `context_pack`
2. inspect `context_diagnostics`
3. inspect `runtime_payload`
4. execute task logic
5. write back structured delta

Do not bypass MCP or the canonical Memory API.
